"""Подагент ``external_doc_loader``: загрузка документов из внешнего источника.

Забирает документы через ``shared.external_source.ExternalDocumentSource``
(массив ``{"direction_id", "text"}``) и по каждому решает, грузить ли его в
``wiki_rag.source_doc``:

* сравнение ВСЕГДА ограничено направлением — документ сверяется только с
  документами своего ``direction_key``;
* точный дубль (sha256 совпал) — пропускаем;
* похожий документ (сходство текста выше порога) — по умолчанию пропускаем и
  показываем в отчёте, с ``replace_similar=true`` заменяем старый (каскад
  снесёт его чанки/кандидатов, новый ляжет pending);
* новый документ — пишем pending (``processed_at IS NULL``), индексацию делает
  ``wiki_ingest``.

Входа не требует: список направлений диктует сам источник. Пороги и режим
замены переопределяются через ``config.configurable``. ORM-модели и сессия БД —
общие с ``easyrag``/``wiki_ingest`` (схема ``wiki_rag``).
"""
