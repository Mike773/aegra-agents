"""Граф ``kb_chat`` — диалоговая «болталка» с корпоративной базой знаний.

Message-driven граф (как ``analytic_orchestrator``): несёт ``messages`` с
``add_messages`` и отвечает ``AIMessage``. На каждый содержательный вопрос
подтягивает релевантные фрагменты из базы знаний через подграф ``easyrag``
(эмбеддинг запроса → pgvector-retrieve по ``direction_key`` → запись gap, если
ничего не нашлось) и отвечает строго по ним. Small-talk (приветствие,
благодарность) роутер уводит мимо базы знаний — отвечаем коротко и по-людски.

История диалога живёт между ходами в per-thread чекпойнтере (в проде — aegra,
в скриптах — ``MemorySaver``): один вызов графа = одно входящее сообщение → ответ.

``direction_key`` читается из ``config.configurable`` (затем из ``state``),
``top_k`` и ``system_prompt_override`` — из ``configurable``.
"""
