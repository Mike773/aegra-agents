"""Подагент `doc_manager`: управление документами направления через чат.

Отдельный message-driven граф. На вход — ``direction_key`` (через
``RunnableConfig.configurable`` или поле state). Одним сообщением пользователь:

* **загружает** документ — тело сообщения сохраняется как ``wiki_rag.source_doc``
  с ``processed_at IS NULL`` (pending). Индексацию (чанки/эмбеддинги/вики-страницы)
  позже делает подагент ``wiki_ingest``;
* просит **список** загруженных документов направления;
* **удаляет** загруженный документ (по номеру из списка, префиксу id или названию).

Намерение классифицирует GigaChat (tool-binding через ``wiki_ingest.llm.LLMClient``).
ORM-модели и сессия БД — общие с ``easyrag``/``wiki_ingest`` (схема ``wiki_rag``).
"""
