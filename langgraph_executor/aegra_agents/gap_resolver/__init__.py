"""Подагент ``gap_resolver``: enrichment-loop по «пробелам» базы знаний.

``easyrag`` при пустом ретриве над wiki пишет строку ``query_gap`` (вопрос задан,
ответа в wiki по направлению нет) — это сигнал для enrichment-loop'а. Данный граф
и есть этот loop: по каждому нерешённому gap он

* ищет ответ в ИСХОДНЫХ документах направления (``source_chunk``) — вдруг данные
  уже загружены, но ещё не попали в wiki;
* заводит пустую wiki-страницу-заглушку по теме вопроса (если её ещё нет);
* помечает gap решённым, если ответ в источниках нашёлся;
* собирает отчёт «что решено, а что нет» и для ненайденного говорит, какие
  документы нужно загрузить.

Вход — ``direction_key`` через ``config.configurable`` (затем из state). ORM-модели
и сессия БД — общие с ``easyrag``/``wiki_ingest`` (схема ``wiki_rag``).
"""
