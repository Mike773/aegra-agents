"""Режим интерактивных подсказок — тексты для промпта и описания инструмента.

Включается входным параметром ``interactive_suggestions``. Всё, что модель
видит об этом режиме, лежит здесь: блок системного промпта и описание
инструмента ``suggest_followups``. При выключенном режиме ни одна из этих
строк в промпт не попадает — он остаётся прежним.
"""
from __future__ import annotations

# Сколько вариантов уходит клиенту: это кнопки под ответом, больше — шум.
MAX_SUGGESTIONS = 3
# Подпись кнопки у клиента бывает длинной — режем только явный перебор.
MAX_LABEL_LEN = 1000

SUGGESTIONS_PROMPT_BLOCK = f"""ВАРИАНТЫ ПРОДОЛЖЕНИЯ (интерактивные подсказки)
Руководитель видит под ответом кнопки с вариантами следующего вопроса. Их даёт
инструмент `suggest_followups`: вызови его ОДИН раз, когда данные собраны и ты
готов писать итог, и сразу после него напиши сам ответ. Передай до
{MAX_SUGGESTIONS} вариантов; у каждого label — подпись кнопки: суть варианта
одной фразой, без точки в конце; content — полный вопрос от лица руководителя,
который он задал бы следующим, с названием показателя, среза или периода. Варианты — это аналитические векторы по данным (посмотреть, разобрать,
сравнить, оценить динамику, уточнить вклад), не рекомендации и не действия.
Первый вариант углубляет главный фокус ответа, остальные — вторая по значимости
зона или достижение, другой срез или период. Раздел «Варианты для углубления»
в тексте ответа НЕ пиши: эти варианты руководитель получит кнопками."""

SUGGESTIONS_TOOL_DESCRIPTION = (
    "Варианты следующего вопроса руководителя — кнопки под ответом. Вызывается "
    f"один раз перед итоговым текстом. Аргумент suggestions — список до "
    f"{MAX_SUGGESTIONS} объектов: label — подпись кнопки, суть варианта одной "
    "фразой; content — полный вопрос от лица руководителя. Только "
    "аналитические векторы по данным, без рекомендаций."
)

__all__ = [
    "MAX_LABEL_LEN",
    "MAX_SUGGESTIONS",
    "SUGGESTIONS_PROMPT_BLOCK",
    "SUGGESTIONS_TOOL_DESCRIPTION",
]
