"""Режим интерактивного графика — тексты для промпта и описания инструмента.

Включается входным параметром ``interactive_chart``. Всё, что модель видит об
этом режиме, лежит здесь: блок системного промпта и описание инструмента
``build_chart``. При выключенном режиме ни одна из этих строк в промпт не
попадает — он остаётся прежним.
"""
from __future__ import annotations

# Пределы фигуры: график под ответом, а не выгрузка данных.
MAX_TRACES = 8
MAX_POINTS = 120

CHART_PROMPT_BLOCK = """ГРАФИК К ОТВЕТУ (интерактивный график)
Руководитель видит под ответом график. Его строит инструмент `build_chart`:
вызови его ОДИН раз, когда данные собраны и ты готов писать итог, и сразу после
него напиши сам ответ. Что показать:
- руководитель описал график (какой показатель, срез, период, сравнение) —
  построй именно его;
- не описал — покажи значения главной проблемной метрики: верхний пункт карты
  отклонений с отрицательной оценкой, факт и план по периодам (линия).
Числа бери ТОЛЬКО из результатов инструментов этого хода — ничего не
придумывай и не округляй сверх того, что вернули данные; нужного ряда нет —
сначала получи его запросом. Период по оси x подписывай датой, серии называй
по-человечески («Факт», «План», «Среднее коллег»). Текстом график не
пересказывай и таблицу с теми же числами не дублируй."""

CHART_TOOL_DESCRIPTION = (
    "График под ответом (plotly). Вызывается один раз перед итоговым текстом. "
    "chart_type — line (динамика), bar (сравнение), scatter (точки); title — "
    "заголовок; x_title, y_title — подписи осей; traces — серии: name — "
    "подпись серии, x — подписи точек (даты, имена, разрезы), y — числа той же "
    "длины (null — пропуск), type — тип серии, если отличается от chart_type. "
    f"До {MAX_TRACES} серий, до {MAX_POINTS} точек в серии. Числа — только из "
    "результатов инструментов."
)

__all__ = [
    "CHART_PROMPT_BLOCK",
    "CHART_TOOL_DESCRIPTION",
    "MAX_POINTS",
    "MAX_TRACES",
]
